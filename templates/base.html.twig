<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Team des Sbires{% endblock %}</title>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">

    <style>
    /* =====  VARIABLES UNIVERS VILAINS  ===== */
    :root{
        --clr-bg:#0d0d17;
        --clr-primary:#00f0ff;
        --clr-secondary:#ff00c1;
        --clr-text:#e5e5e5;
        --clr-accent:var(--clr-primary);
        --ff-body:'Orbitron',monospace;
        --fw-body:400;
        --lh-body:1.5;
    }

    /* =====  RESET + BASE  ===== */
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{
        background:var(--clr-bg);
        color:var(--clr-text);
        font-family:var(--ff-body);
        line-height:var(--lh-body);
        min-height:100vh;
        display:flex;
        flex-direction:column;
        overflow-x:hidden;
    }

    /* =====  SCANLINES LÉGÈRES  ===== */
    body::before{
        content:"";
        position:fixed;
        inset:0;
        pointer-events:none;
        z-index:9999;
        background:repeating-linear-gradient(
            to bottom,
            rgba(0,240,255,.03) 0 1px,
            transparent 1px 3px
        );
        animation:scan 2s linear infinite;
    }
    @keyframes scan{to{background-position:0 3px}}

    /* =====  NAVBAR GLASS  ===== */
    .poke-banner{
        display:flex;
        align-items:center;
        justify-content:space-between;
        padding:1.2rem 2rem;
        background:rgba(0,0,0,.35);
        border-bottom:2px solid var(--clr-primary);
        backdrop-filter:blur(10px);
    }
    .poke-banner h1{
        font-size:1.6rem;
        color:var(--clr-primary);
        text-shadow:0 0 8px;
    }
    .poke-nav{display:flex;gap:.8rem;font-size:.9rem}
    .btn-nav{
        padding:.5rem 1rem;
        border:1px solid var(--clr-primary);
        background:transparent;
        color:var(--clr-primary);
        border-radius:4px;
        text-decoration:none;
        transition:.25s;
        box-shadow:0 0 6px -2px var(--clr-primary);
    }
    .btn-nav:hover{
        background:var(--clr-primary);
        color:#000;
        box-shadow:0 0 12px 0 var(--clr-primary);
    }
    .nav-welcome{font-size:.8rem;color:#fff;margin-right:.5rem}

    /* =====  FOOTER  ===== */
    footer{
        margin-top:auto;
        text-align:center;
        padding:1.5rem 0;
        font-size:.7rem;
        color:var(--clr-primary);
        text-shadow:0 0 4px;
    }
    </style>

    {% block stylesheets %}{{ encore_entry_link_tags('app') }}{% endblock %}
</head>

<body>
    {% block navigation %}
    <nav class="poke-banner">
        <h1>Rejoignez les Teams de Vilains</h1>
        <div class="poke-nav">
            {% if app.user %}
                <span class="nav-welcome">Salut, <strong>{{ app.user.codename }}</strong></span>
                <a href="{{ path('app_logout') }}" class="btn-nav">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn-nav">Connexion</a>
                <a href="{{ path('app_register') }}" class="btn-nav">S’inscrire</a>
            {% endif %}
        </div>
    </nav>
    {% endblock %}

    <main id="root">{% block body %}{% endblock %}</main>

    {% block javascripts %}{% endblock %}
</body>
</html>