{% extends 'base.html.twig' %}

{% block title %}Premier vol – Team Rocket{% endblock %}

{% block body %}
    <div id="react-root"></div>

    {# Bloc mission unique (rempli par JS) #}
    <div id="mission-block" class="mb-4" style="display:none;"></div>

    {# Zone du mini-jeu #}
    <div id="game-area" class="mt-4"></div>
    <p class="mt-3">Temps : <span id="timer">00:30</span> | Tentatives : <span id="attempts-count">0</span></p>
    <button id="start-btn" class="btn btn-warning">Lancer le vol</button>
    <div id="result" class="mt-3"></div>

    <script>
        const csrfToken = "{{ csrf_token }}";
    </script>
    <script src="{{ asset('js/game_first_theft.js') }}" defer></script>
{% endblock %}