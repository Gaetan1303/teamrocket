<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Team Rocket - Recrutement{% endblock %}</title>

    <style>
        :root {
            --team-rocket-red: #cc0000;
            --team-rocket-black: #222;
            --bg-dark: #121212;
            --text-light: #eee;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            background: var(--bg-dark);
            color: var(--text-light);
            font-family: 'Roboto', sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        header {
            background: var(--team-rocket-black);
            border-bottom: 2px solid var(--team-rocket-red);
            padding: 1rem 2rem;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav .brand {
            font-family: 'Wallpoet', cursive;
            font-size: 1.5rem;
            letter-spacing: 2px;
            color: var(--team-rocket-red);
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 1.5rem;
        }

        nav a {
            color: var(--text-light);
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s;
        }

        nav a:hover {
            color: var(--team-rocket-red);
        }

        main {
            flex: 1;
            padding: 2rem;
        }

        footer {
            text-align: center;
            padding: 1rem;
            font-size: 0.8rem;
            color: #aaa;
            background: #0a0a0a;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

<body>
    <header>
        <nav>
            <div class="brand">Team Rocket</div>
            <ul>
                <li><a href="{{ path('app_home') }}">Accueil</a></li>

                {% if app.user %}
                    <li><span>Bienvenue, {{ app.user.codename }}</span></li>
                    <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                {% else %}
                    <li><a href="{{ path('app_register') }}">Inscription</a></li>
                    <li><a href="{{ path('app_login') }}">Connexion</a></li>
                {% endif %}
            </ul>
        </nav>
    </header>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer>
        &copy; {{ "now"|date("Y") }} Team Rocket. Tous droits de conquête mondiaux réservés.
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>